/**
 * Write a function called `toCamelCase` that takes a string as input and returns the camelCase version of that string.
 * The function should:
 * - Remove any non-alphanumeric characters (except spaces and underscores).
 * - Convert the first word to lowercase.
 * - Capitalize the first letter of each subsequent word.
 * - Remove all spaces and underscores.
 *
 * Example:
 * toCamelCase("hello world") // returns "helloWorld"
 * toCamelCase("Convert_this-string") // returns "convertThisString"
 */